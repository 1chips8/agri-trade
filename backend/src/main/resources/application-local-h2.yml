spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:agri_trade;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
  rabbitmq:
    host: localhost
    port: 5672
    username: agri
    password: agri123456
  redis:
    host: localhost
    port: 6379

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

agri:
  order:
    timeout:
      enabled: false
